<Window x:Class="MinecraftServerManager.App.Views.RestoreBackupDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding WindowTitle}"
        Width="640"
        Height="460"
        MinWidth="520"
        MinHeight="380"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource WindowBrush}"
        Icon="pack://application:,,,/MinecraftServerManager.App;component/assets/icon.ico">
  <Grid Margin="24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <!-- 標題與說明 -->
    <StackPanel Grid.Row="0" Margin="0,0,0,16">
      <TextBlock Text="備份還原與管理" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource TextBrush}" />
      <TextBlock Text="選擇備份存檔進行還原（具備 Staging 交易安全防護與自動復原機制）"
                 Foreground="{DynamicResource MutedTextBrush}" FontSize="12" Margin="0,4,0,0" />
    </StackPanel>

    <!-- 備份表格 -->
    <Border Grid.Row="1"
            Background="{DynamicResource CardBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="6"
            Margin="0,0,0,14">
      <DataGrid ItemsSource="{Binding Backups}"
                SelectedItem="{Binding SelectedBackup}"
                AutoGenerateColumns="False"
                IsReadOnly="True"
                SelectionMode="Single"
                GridLinesVisibility="None"
                HeadersVisibility="Column"
                RowHeaderWidth="0">
        <DataGrid.Columns>
          <DataGridTextColumn Header="備份檔案名稱" Binding="{Binding FileName}" Width="*" />
          <DataGridTextColumn Header="檔案大小" Binding="{Binding SizeFormatted}" Width="100" />
          <DataGridTextColumn Header="建立時間" Binding="{Binding CreatedAtFormatted}" Width="160" />
        </DataGrid.Columns>
      </DataGrid>
    </Border>

    <!-- 狀態提示 -->
    <TextBlock Grid.Row="2"
               Text="{Binding StatusMessage}"
               Foreground="{DynamicResource MutedTextBrush}"
               FontSize="12"
               Margin="0,0,0,12" />

    <!-- 底部按鈕區 -->
    <Grid Grid.Row="3">
      <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
        <Button Content="刪除選取備份"
                Command="{Binding DeleteCommand}"
                Style="{DynamicResource DangerButtonStyle}"
                Padding="14,6" />
      </StackPanel>

      <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
        <Button Content="取消"
                Command="{Binding CancelCommand}"
                Margin="0,0,10,0"
                Padding="16,6" />
        <Button Content="開始還原"
                Command="{Binding RestoreCommand}"
                Style="{DynamicResource PrimaryButtonStyle}"
                Padding="20,6" />
      </StackPanel>
    </Grid>
  </Grid>
</Window>
